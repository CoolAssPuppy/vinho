node_modules
.env*
# Committed templates documenting which variables each app needs (no secrets).
!.env.example
!**/.env.example
dist
build
.next
coverage
.vscode
.idea
*.swp
*.swo
.DS_Store
Thumbs.db
.supabase
supabase/.branches
supabase/.temp
playwright-report
playwright-results
test-results
xcuserdata
DerivedData
.claude/settings.local.json
CLAUDE.local.md
mcp.json
.mcp.json
.turbo/
scripts/.venv/

# iOS Configuration files with secrets
Config.local.xcconfig
*.local.xcconfig
apps/vinho-ios/Vinho/Config.plist

# Android release files materialized from Doppler
apps/vinho-android/local.properties
apps/vinho-android/play-service-account.json
apps/vinho-android/app/keystore/

# Apple Sign in with Apple keys
*.p8

# NOTE: gradle/wrapper/gradle-wrapper.jar is deliberately NOT ignored. The Gradle
# wrapper jar must be committed or `./gradlew` fails on a fresh clone with
# "Could not find or load main class org.gradle.wrapper.GradleWrapperMain".
