{
  "name": "vinho",
  "private": true,
  "packageManager": "pnpm@9.12.2",
  "scripts": {
    "dev": "if command -v doppler >/dev/null 2>&1; then doppler run -- next dev; else next dev; fi",
    "build": "if command -v doppler >/dev/null 2>&1; then doppler run -- next build; else next build; fi",
    "start": "if command -v doppler >/dev/null 2>&1; then doppler run -- next start; else next start; fi",
    "lint": "if command -v doppler >/dev/null 2>&1; then doppler run -- next lint --fix; else next lint --fix; fi",
    "doctor": "node scripts/doctor.mjs",
    "supa:push": "supabase db push",
    "supa:types": "supabase gen types typescript --project-id aghiopwrzzvamssgcwpv > packages/db-types/src/database.types.ts",
    "supa:dump": "supabase db dump --project-ref aghiopwrzzvamssgcwpv > supabase/vinho.sql"
  },
  "devDependencies": {
    "@types/node": "^22.5.1",
    "dotenv": "^17.2.2",
    "eslint": "^9.9.0",
    "eslint-config-prettier": "^9.1.0",
    "eslint-plugin-import": "^2.29.1",
    "eslint-plugin-promise": "^6.4.0",
    "eslint-plugin-unused-imports": "^4.0.0",
    "husky": "^9.1.5",
    "json": "^11.0.0",
    "lint-staged": "^15.2.10",
    "prettier": "^3.3.3",
    "tsx": "^4.20.6",
    "turbo": "^2.0.6",
    "typescript": "^5.6.2",
    "zx": "^8.1.4"
  },
  "lint-staged": {
    "**/*.{ts,tsx,js,jsx,css,md,json}": [
      "prettier --write"
    ],
    "**/*.{ts,tsx,js,jsx}": [
      "eslint --fix"
    ]
  }
}
