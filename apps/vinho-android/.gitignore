# Gradle
.gradle/
build/
local.properties

# Android Studio
.idea/
*.iml

# NOTE: gradle/wrapper/gradle-wrapper.jar is deliberately committed. Ignoring it
# breaks `./gradlew` on a fresh clone ("Could not find or load main class
# org.gradle.wrapper.GradleWrapperMain"). It is a 42 KB bootstrap jar and is
# expected to be in version control.

# App artifacts
app/build/
*.apk
*.aab
captures/

# Kotlin toolchains
.kotlin/

# Android release secrets (materialized from Doppler)
play-service-account.json
*.jks
app/keystore/
